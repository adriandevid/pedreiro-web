
services:

#start registry
  registry: 
    image: registry:3 
    container_name: registry 
    restart: always 
    ports: 
      - 5000:5000 

#end registry
#[content]
        
networks:
  web:
    external: false
